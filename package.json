{
  "name": "pocket-monsters",
  "description": "Send Pokemon releases to transmission in a parsable format",
  "main": "dist/index.js",
  "private": true,
  "author": "Kevin Richter<me@kevinrichter.nl>",
  "license": "MIT",
  "scripts": {
    "prestart": "npm run build",
    "start": "node dist",
    "scheduled": "npm run start -- --scheduled --in-memory",
    "build": "tsc"
  },
  "dependencies": {
    "@types/cron": "^2.0.0",
    "@types/node": "^18.0.0",
    "cron": "^2.0.0",
    "deepmerge": "^4.2.2",
    "dotenv": "^16.0.1",
    "dotenv-expand": "^8.0.3",
    "nyaapi": "^2.0.5",
    "transmission": "^0.4.9",
    "typescript": "4.7"
  },
  "engines": {
    "node": ">=7.6.0",
    "npm": ">=5.0.0"
  }
}
